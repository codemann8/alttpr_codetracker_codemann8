-- OVERRIDE THIS FILE if you wish to modify code from scripts/logic.lua, then only include those functions you want changed here

-- Example: If you wanted to make curtains only accessible in swordless mode (useless example, but this is how you'd override it)
-- function canRemoveCurtains()
--     if Tracker:ProviderCountForCode("swordless") > 0 then
--         return 1
--     else
--         return 0
--     end
-- end
